import { Injectable } from '@nestjs/common';
import { IntegerType } from 'typeorm';

@Injectable()
export class AuthService {
  /*
  constructor(
    private readonly instructorService : InstructorService,
    private readonly studentService : StudentService,
  )

  signupInstructor(instructorDto: CreateInstructorDto) {
    
  }

  signupStudent(studentDto: CreateStudentDto) {
  
  }

  login(credentials: { email: string; password: string }){
      if(this.validateUser(credentials.email, credentials.password) == null){
        return 
      }
  }

  private validateUser(email: string, password: string){
      
  }
  */
}
